function monitorCount(rows, columns){
  return rows*columns;
}

const numOfMonitors = monitorCount(5, 4);

console.log(numOfMonitors);

//This shows how to return a value from your function so you can use that value elsewhewre in your script.
