const plantNeedsWater = day => day === 'Wednesday' ? true : false;

//This shows how to chop the code down quite a bit on an arrow function, to the point you don't need the function keyword, parenthesis around a single argument, or a return statement.
