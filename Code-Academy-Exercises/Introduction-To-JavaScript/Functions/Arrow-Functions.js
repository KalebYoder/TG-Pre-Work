const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};
//This shows how to use arrow expressions "Fat Arrow Notation" as the page puts it, which is another form of a function expression creating an anonymous function.
