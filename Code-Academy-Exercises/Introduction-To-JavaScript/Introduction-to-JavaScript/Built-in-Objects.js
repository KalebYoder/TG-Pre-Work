console.log(Math.floor(Math.random()*100));

console.log(Math.ceil(43.8));

console.log(Number.isInteger(2017));

//This lesson shows how to use methods from the built in Math and Number objects to manipulate numbers and log results, including nesting methods within methods.
